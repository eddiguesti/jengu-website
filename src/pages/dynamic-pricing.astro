---
import BaseLayout from '../components/layout/BaseLayout.astro';
import { generateSEO } from '../lib/seo/metadata';

const seo = generateSEO({
  title: 'Dynamic Pricing Software for Campsites | Maximize Revenue with AI',
  description: 'Smart dynamic pricing system for campsites and RV parks. Increase revenue by up to 40% with AI-powered pricing that adapts to demand, seasonality, and competition.',
  canonical: '/dynamic-pricing',
  ogType: 'website'
});
---

<BaseLayout seo={seo}>
  <!-- Hero Section -->
  <section class="hero hero-pricing">
    <!-- Desktop Hero -->
    <div class="hero-container-pricing desktop-only">
      <div class="hero-content-pricing">
        <span class="hero-badge">
          <i class="fas fa-chart-line"></i>
          Dynamic Pricing for Campsites
        </span>
        <h1>
          Maximize Your Campsite Revenue with <span class="gradient-text">AI-Powered Pricing</span>
        </h1>
        <p class="hero-description">
          Our dynamic pricing system automatically adjusts your campsite rates based on demand, seasonality, weather, events, and competition—increasing revenue by up to 40% while keeping occupancy high.
        </p>

        <div class="hero-stats-mini">
          <div class="mini-stat">
            <strong>Up to 40%</strong>
            <span>Revenue Increase</span>
          </div>
          <div class="mini-stat">
            <strong>24/7</strong>
            <span>Automated</span>
          </div>
          <div class="mini-stat">
            <strong>Real-time</strong>
            <span>Price Updates</span>
          </div>
        </div>

        <div class="hero-cta-group">
          <a href="/contact" class="btn btn-primary btn-large">
            <i class="fas fa-calendar-check"></i>
            Get Free Demo
          </a>
          <a href="#how-it-works" class="btn btn-secondary btn-large">
            <i class="fas fa-play-circle"></i>
            See How It Works
          </a>
        </div>

        <p class="hero-trust">
          <i class="fas fa-check-circle"></i>
          Free consultation & demo
        </p>
      </div>

      <!-- App Screenshots Collage -->
      <div class="hero-visual-pricing">
        <div class="screenshots-collage">
          <div class="screenshot-card screenshot-1" onclick="openImageModal('/images/price-optimization.png')">
            <picture>
              <source srcset="/images/price-optimization.webp" type="image/webp">
              <img src="/images/price-optimization.png" alt="Price Optimization Dashboard" loading="eager">
            </picture>
          </div>
          <div class="screenshot-card screenshot-2" onclick="openImageModal('/images/forecasting.png')">
            <picture>
              <source srcset="/images/forecasting.webp" type="image/webp">
              <img src="/images/forecasting.png" alt="Revenue Forecasting" loading="eager">
            </picture>
          </div>
          <div class="screenshot-card screenshot-3" onclick="openImageModal('/images/pricing-strategy.png')">
            <picture>
              <source srcset="/images/pricing-strategy.webp" type="image/webp">
              <img src="/images/pricing-strategy.png" alt="Pricing Strategy" loading="eager">
            </picture>
          </div>
          <div class="screenshot-card screenshot-4" onclick="openImageModal('/images/competitor-discovery.png')">
            <picture>
              <source srcset="/images/competitor-discovery.webp" type="image/webp">
              <img src="/images/competitor-discovery.png" alt="Competitor Analysis" loading="eager">
            </picture>
          </div>
        </div>
      </div>
    </div>

    <!-- Mobile only: Fullscreen Video Hero -->
    <div class="mobile-only video-bg">
      <video autoplay muted playsinline loop id="pricing-bg-video">
        <source src="/videos/mobile-hero.mp4" type="video/mp4">
      </video>

      <!-- Gradient overlay -->
      <div class="mobile-hero-gradient"></div>

      <!-- Content overlay -->
      <div class="mobile-hero-overlay">
        <h1 class="mobile-hero-headline">
          Maximize Your Revenue with <span class="gradient-text">AI Pricing</span>
        </h1>
        <div class="mobile-subhead">Smart dynamic pricing for campsites. Increase revenue by up to 40% with automated pricing adjustments.</div>
        <div class="mobile-hero-actions">
          <a href="#how-it-works" class="mobile-hero-btn mobile-hero-btn-primary">
            Check Out How It Works
          </a>
          <a href="/contact" class="mobile-hero-btn mobile-hero-btn-secondary">
            Get Free Demo
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Problem Section -->
  <section class="section-dark">
    <div class="container-narrow">
      <div class="section-header">
        <h2>Stop Leaving Money on the Table</h2>
        <p>Traditional flat-rate pricing costs campsite owners thousands in lost revenue every season</p>
      </div>

      <div class="problems-creative-grid">
        <div class="problem-card-creative problem-primary">
          <div class="problem-number">01</div>
          <div class="problem-icon-creative">
            <i class="fas fa-calendar-times"></i>
          </div>
          <h3>Peak Season Underpricing</h3>
          <p>You could charge <span class="highlight">2-3x more</span> during holidays and weekends, but stick to flat rates—leaving <span class="highlight-negative">thousands in revenue</span> on the table</p>
          <div class="problem-impact">
            <span class="impact-label">Lost Revenue:</span>
            <span class="impact-value">€15,000+/season</span>
          </div>
        </div>

        <div class="problem-card-creative">
          <div class="problem-number">02</div>
          <div class="problem-icon-creative">
            <i class="fas fa-bed"></i>
          </div>
          <h3>Empty Sites in Low Season</h3>
          <p>Lower prices could fill empty sites and generate revenue, but manual adjustments are <span class="highlight-negative">time-consuming</span> and you're never sure if you're pricing optimally</p>
        </div>

        <div class="problem-card-creative">
          <div class="problem-number">03</div>
          <div class="problem-icon-creative">
            <i class="fas fa-users-slash"></i>
          </div>
          <h3>Missing Market Signals</h3>
          <p>Local events, weather forecasts, and competitor pricing change <span class="highlight">daily</span>—but your rates stay <span class="highlight-negative">static</span>, causing you to miss opportunities</p>
        </div>

        <div class="problem-card-creative problem-accent">
          <div class="problem-number">04</div>
          <div class="problem-icon-creative">
            <i class="fas fa-clock"></i>
          </div>
          <h3>Manual Price Management</h3>
          <p><span class="highlight-negative">Hours wasted every week</span> updating rates across multiple booking platforms instead of focusing on guest experience</p>
          <div class="problem-impact">
            <span class="impact-label">Time Lost:</span>
            <span class="impact-value">10+ hrs/week</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Platform Showcase: How It Works -->
  <section class="platform-showcase-section" id="how-it-works">
    <div class="container">
      <div class="section-header">
        <span class="section-badge">
          <i class="fas fa-cogs"></i>
          Inside the Platform
        </span>
        <h2>See How Dynamic Pricing Works</h2>
        <p>Explore the AI-powered features that make intelligent campsite pricing possible</p>
      </div>

      <div class="screenshot-grid">
        <!-- Step 1: Connect PMS & Upload Data -->
        <div class="screenshot-item">
          <img src="/images/data-enrichment.png" alt="Connect PMS and Upload Historical Data" class="screenshot-img" loading="lazy" onclick="openImageModal(this.src)">
          <div class="screenshot-caption">
            <h4><i class="fas fa-upload"></i> Step 1: Connect Your PMS & Upload Data</h4>
            <p>Simply connect your existing Property Management System or upload your historical booking data (CSV, Excel). The platform instantly imports years of reservations, pricing history, and occupancy patterns. Once uploaded, our AI begins analyzing your past performance to understand what drives bookings at your campsite—no complex setup required, just plug and play.</p>
            <div class="feature-benefits-inline">
              <span><i class="fas fa-check"></i> Direct PMS integration</span>
              <span><i class="fas fa-check"></i> CSV/Excel upload</span>
              <span><i class="fas fa-check"></i> Automatic data import</span>
            </div>
          </div>
        </div>

        <!-- Step 2: AI Analysis & Correlation Discovery -->
        <div class="screenshot-item screenshot-reverse">
          <img src="/images/price-optimization.png" alt="AI Analysis Dashboard" class="screenshot-img" loading="lazy" onclick="openImageModal(this.src)">
          <div class="screenshot-caption">
            <h4><i class="fas fa-brain"></i> Step 2: AI Discovers Pricing Correlations</h4>
            <p>The platform enriches your historical data with weather conditions (temperature, rain, sunshine), holiday calendars, school breaks, local events, and seasonality patterns. Machine learning algorithms then identify powerful correlations: "Sunny weekends in July drive 40% higher bookings" or "Competitor price drops reduce our occupancy by 15%." These insights become the foundation for intelligent pricing decisions tailored specifically to your campsite's unique patterns.</p>
            <div class="feature-benefits-inline">
              <span><i class="fas fa-check"></i> Weather correlation analysis</span>
              <span><i class="fas fa-check"></i> Holiday & season patterns</span>
              <span><i class="fas fa-check"></i> Capacity optimization insights</span>
            </div>
          </div>
        </div>

        <!-- Step 3: Track Competitors -->
        <div class="screenshot-item">
          <img src="/images/competitor-discovery.png" alt="Competitor Tracking Interface" class="screenshot-img" loading="lazy" onclick="openImageModal(this.src)">
          <div class="screenshot-caption">
            <h4><i class="fas fa-binoculars"></i> Step 3: Follow Your Competition</h4>
            <p>The system automatically discovers nearby campsites within your custom radius using camping-and-co.com data and monitors their pricing strategies 24/7. See exactly what your competitors charge for similar dates, track their occupancy levels, and receive alerts when they make significant price changes. Never get undercut or lose bookings to competitors again—you'll always know where you stand in the market.</p>
            <div class="feature-benefits-inline">
              <span><i class="fas fa-check"></i> Automatic competitor discovery</span>
              <span><i class="fas fa-check"></i> Real-time price tracking</span>
              <span><i class="fas fa-check"></i> Market position alerts</span>
            </div>
          </div>
        </div>

        <!-- Step 4: Configure Pricing Strategy -->
        <div class="screenshot-item screenshot-reverse">
          <img src="/images/pricing-strategy.png" alt="Configure Dynamic Pricing Strategy" class="screenshot-img" loading="lazy" onclick="openImageModal(this.src)">
          <div class="screenshot-caption">
            <h4><i class="fas fa-sliders-h"></i> Step 4: Tweak Your Pricing Strategy</h4>
            <p>You're in full control. Choose from Conservative (steady prices, maximize occupancy), Balanced (optimal revenue/occupancy mix), or Aggressive (maximum revenue extraction) strategies. Fine-tune how aggressively the AI responds to demand spikes, set minimum and maximum price limits, define occupancy targets, and decide how much weight to give competitor pricing. The AI learns your preferences and optimizes within your rules—you set the guardrails, AI does the heavy lifting.</p>
            <div class="feature-benefits-inline">
              <span><i class="fas fa-check"></i> 3 pricing strategies</span>
              <span><i class="fas fa-check"></i> Custom sensitivity controls</span>
              <span><i class="fas fa-check"></i> Min/max price limits</span>
            </div>
          </div>
        </div>

        <!-- Step 5: Beautiful Calendar Interface -->
        <div class="screenshot-item">
          <img src="/images/forecasting.png" alt="Beautiful Calendar Pricing Interface" class="screenshot-img" loading="lazy" onclick="openImageModal(this.src)">
          <div class="screenshot-caption">
            <h4><i class="fas fa-calendar-alt"></i> Step 5: Manage Prices on a Beautiful Calendar</h4>
            <p>All your pricing comes together in a stunning, intuitive calendar interface. See AI-recommended prices for the next 60 days, color-coded by demand level and revenue potential. Hover over any date to see why the AI suggests that price (weather forecast, competitor rates, holidays, historical patterns). Don't like a recommendation? Click to override manually. Drag to bulk-edit multiple dates. It's visual, fast, and gives you complete visibility and control over your entire pricing strategy at a glance.</p>
            <div class="feature-benefits-inline">
              <span><i class="fas fa-check"></i> 60-day visual calendar</span>
              <span><i class="fas fa-check"></i> One-click manual overrides</span>
              <span><i class="fas fa-check"></i> Bulk editing & drag controls</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Results Section -->
  <section class="stats-section">
    <div class="container">
      <div class="section-header">
        <h2>Proven Results for Campsite Owners</h2>
        <p>Real data from campsites using our dynamic pricing system</p>
      </div>

      <div class="stats-container">
        <div class="stat-card">
          <span class="stat-number">+38%</span>
          <span class="stat-label">Average Revenue Increase</span>
        </div>
        <div class="stat-card">
          <span class="stat-number">+15%</span>
          <span class="stat-label">Occupancy Improvement</span>
        </div>
        <div class="stat-card">
          <span class="stat-number">10hrs</span>
          <span class="stat-label">Saved Weekly</span>
        </div>
        <div class="stat-card">
          <span class="stat-number">24/7</span>
          <span class="stat-label">Automatic Optimization</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Pricing Factors -->
  <section class="section-light">
    <div class="container">
      <div class="section-header">
        <span class="section-badge">
          <i class="fas fa-lightbulb"></i>
          Smart Factors
        </span>
        <h2>What Influences Your Prices?</h2>
        <p>Our AI considers 20+ data points to calculate the optimal price</p>
      </div>

      <div class="factors-hexagon">
        <!-- Row 1: 2 items -->
        <div class="hex-row hex-row-1">
          <div class="factor-item">
            <i class="fas fa-calendar-alt"></i>
            <span>Booking lead time</span>
          </div>
          <div class="factor-item">
            <i class="fas fa-sun"></i>
            <span>Weather forecasts</span>
          </div>
        </div>

        <!-- Row 2: 3 items -->
        <div class="hex-row hex-row-2">
          <div class="factor-item">
            <i class="fas fa-chart-line"></i>
            <span>Historical demand</span>
          </div>
          <div class="factor-item">
            <i class="fas fa-users"></i>
            <span>Current occupancy</span>
          </div>
          <div class="factor-item">
            <i class="fas fa-calendar-week"></i>
            <span>Day of week</span>
          </div>
        </div>

        <!-- Row 3: 4 items (widest) -->
        <div class="hex-row hex-row-3">
          <div class="factor-item">
            <i class="fas fa-snowflake"></i>
            <span>Season & holidays</span>
          </div>
          <div class="factor-item">
            <i class="fas fa-music"></i>
            <span>Local events</span>
          </div>
          <div class="factor-item">
            <i class="fas fa-school"></i>
            <span>School vacations</span>
          </div>
          <div class="factor-item">
            <i class="fas fa-store"></i>
            <span>Competitor rates</span>
          </div>
        </div>

        <!-- Row 4: 3 items -->
        <div class="hex-row hex-row-4">
          <div class="factor-item">
            <i class="fas fa-clock"></i>
            <span>Time until arrival</span>
          </div>
          <div class="factor-item">
            <i class="fas fa-moon"></i>
            <span>Length of stay</span>
          </div>
          <div class="factor-item">
            <i class="fas fa-rocket"></i>
            <span>Booking velocity</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="cta-content">
      <h2>Ready to Increase Your Campsite Revenue?</h2>
      <p>Get a free consultation and see how much more you could be earning with dynamic pricing</p>
      <div class="cta-buttons">
        <a href="/contact" class="btn btn-primary btn-large">
          <i class="fas fa-calendar-check"></i>
          Schedule Free Demo
        </a>
        <a href="#" onclick="handleCalculatorClick(event)" class="btn btn-secondary btn-large">
          <i class="fas fa-calculator"></i>
          Calculate Potential ROI
        </a>
      </div>
      <p class="cta-note">
        <i class="fas fa-shield-alt"></i>
        No credit card required • Free setup consultation • 30-day money-back guarantee
      </p>
    </div>
  </section>

  <!-- Image Modal -->
  <div class="image-modal" id="imageModal" onclick="closeImageModal()">
    <button class="modal-close" onclick="closeImageModal()">
      <i class="fas fa-times"></i>
    </button>
    <img src="" alt="Platform Screenshot" class="modal-content-img" id="modalImage">
  </div>

  <!-- JavaScript for Image Modal -->
  <script is:inline>
    function openImageModal(src) {
      const modal = document.getElementById('imageModal');
      const modalImg = document.getElementById('modalImage');
      modal.classList.add('active');
      modalImg.src = src;
      document.body.style.overflow = 'hidden';
    }

    function closeImageModal() {
      const modal = document.getElementById('imageModal');
      modal.classList.remove('active');
      document.body.style.overflow = 'auto';
    }

    // Close modal on Escape key
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') {
        closeImageModal();
      }
    });
  </script>

  <!-- Calculator Modal -->
  <div id="calculatorModalOverlay" class="calculator-modal-overlay" onclick="if(event.target === this) closeCalculatorModal()">
    <div class="calculator-modal-content">
      <button class="calculator-modal-close" onclick="closeCalculatorModal()">
        <i class="fas fa-times"></i>
      </button>
      <iframe
        src="/calculator-ai-agents-2025.html"
        class="calculator-iframe"
        title="AI ROI Calculator">
      </iframe>
    </div>
  </div>
</BaseLayout>

<style>
  .hero-pricing {
    background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 100%);
    position: relative;
    overflow: hidden;
  }

  .hero-pricing::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -20%;
    width: 800px;
    height: 800px;
    background: radial-gradient(circle, rgba(6, 182, 212, 0.15) 0%, transparent 70%);
    border-radius: 50%;
    filter: blur(80px);
  }

  .hero-container-pricing {
    max-width: 1400px;
    margin: 0 auto;
    padding: 8rem 2rem 6rem;
    display: flex;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 2;
  }

  .hero-content-pricing {
    flex: 1;
    max-width: 600px;
  }

  .hero-visual-pricing {
    flex: 1;
    position: relative;
    min-width: 500px;
  }

  /* Screenshot Collage - Clean Grid Layout */
  .screenshots-collage {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    grid-template-rows: repeat(2, 1fr);
    gap: 1.5rem;
    width: 100%;
    height: 550px;
    padding: 1rem;
  }

  .screenshot-card {
    position: relative;
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4);
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    border: 2px solid rgba(6, 182, 212, 0.4);
    background: linear-gradient(135deg, rgba(6, 182, 212, 0.08), rgba(30, 64, 175, 0.08));
    cursor: pointer;
  }

  /* Glowing border animation */
  .screenshot-card::before {
    content: '';
    position: absolute;
    inset: -2px;
    background: linear-gradient(135deg, #06B6D4, #1E40AF, #06B6D4);
    border-radius: 16px;
    opacity: 0;
    z-index: -1;
    filter: blur(10px);
    transition: opacity 0.4s ease;
    animation: border-glow 3s linear infinite;
    background-size: 200% 200%;
  }

  .screenshot-card:hover::before {
    opacity: 0.6;
  }

  @keyframes border-glow {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  /* Shimmer effect */
  .screenshot-card::after {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    animation: shimmer 3s infinite;
  }

  @keyframes shimmer {
    0% { left: -100%; }
    100% { left: 100%; }
  }

  .screenshot-card:hover {
    transform: translateY(-12px) scale(1.03);
    box-shadow: 0 25px 70px rgba(6, 182, 212, 0.5);
    border-color: var(--secondary-brand);
  }

  .screenshot-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.4s ease;
  }

  .screenshot-card:hover img {
    transform: scale(1.08);
    filter: brightness(1.1);
  }

  /* Individual card animations with floating and rotation */
  .screenshot-1 {
    animation: float-bounce-1 5s ease-in-out infinite;
  }

  .screenshot-2 {
    animation: float-bounce-2 6s ease-in-out infinite;
  }

  .screenshot-3 {
    animation: float-bounce-3 5.5s ease-in-out infinite;
  }

  .screenshot-4 {
    animation: float-bounce-4 6.5s ease-in-out infinite;
  }

  /* Floating animations with subtle rotation */
  @keyframes float-bounce-1 {
    0%, 100% {
      transform: translateY(0px) rotate(0deg);
    }
    25% {
      transform: translateY(-15px) rotate(1deg);
    }
    50% {
      transform: translateY(-8px) rotate(-0.5deg);
    }
    75% {
      transform: translateY(-12px) rotate(0.5deg);
    }
  }

  @keyframes float-bounce-2 {
    0%, 100% {
      transform: translateY(0px) rotate(0deg);
    }
    25% {
      transform: translateY(-10px) rotate(-1deg);
    }
    50% {
      transform: translateY(-18px) rotate(0.5deg);
    }
    75% {
      transform: translateY(-6px) rotate(-0.5deg);
    }
  }

  @keyframes float-bounce-3 {
    0%, 100% {
      transform: translateY(0px) rotate(0deg);
    }
    25% {
      transform: translateY(-12px) rotate(0.5deg);
    }
    50% {
      transform: translateY(-5px) rotate(-1deg);
    }
    75% {
      transform: translateY(-16px) rotate(0.5deg);
    }
  }

  @keyframes float-bounce-4 {
    0%, 100% {
      transform: translateY(0px) rotate(0deg);
    }
    25% {
      transform: translateY(-8px) rotate(-0.5deg);
    }
    50% {
      transform: translateY(-14px) rotate(1deg);
    }
    75% {
      transform: translateY(-10px) rotate(-0.5deg);
    }
  }

  /* Platform Showcase Section */
  .platform-showcase-section {
    background: var(--bg-main);
    padding: 6rem 2rem;
  }

  .screenshot-grid {
    display: flex;
    flex-direction: column;
    gap: 6rem;
    margin-top: 4rem;
    max-width: 1400px;
    margin-left: auto;
    margin-right: auto;
  }

  .screenshot-item {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    max-width: 1400px;
    margin: 0 auto;
  }

  .screenshot-reverse {
    direction: rtl;
  }

  .screenshot-reverse > * {
    direction: ltr;
  }

  .screenshot-img {
    width: 100%;
    height: auto;
    border-radius: 12px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    border: 1px solid var(--border-light);
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .screenshot-img:hover {
    transform: translateY(-8px);
    box-shadow: 0 25px 70px rgba(6, 182, 212, 0.4);
    border-color: var(--secondary-brand);
  }

  .screenshot-caption h4 {
    font-size: 1.5rem;
    color: var(--text-primary);
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .screenshot-caption h4 i {
    color: var(--secondary-brand);
    font-size: 1.3rem;
  }

  .screenshot-caption p {
    color: var(--text-secondary);
    line-height: 1.8;
    font-size: 1.05rem;
    margin-bottom: 1.5rem;
  }

  .feature-benefits-inline {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: 1rem;
  }

  .feature-benefits-inline span {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(6, 182, 212, 0.1);
    padding: 0.5rem 1rem;
    border-radius: 8px;
    font-size: 0.95rem;
    color: var(--text-primary);
    border: 1px solid rgba(6, 182, 212, 0.2);
  }

  .feature-benefits-inline span i {
    color: var(--success);
    font-size: 0.85rem;
  }

  /* Image Modal */
  .image-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(0, 0, 0, 0.95);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    cursor: pointer;
  }

  .image-modal.active {
    opacity: 1;
    visibility: visible;
  }

  .modal-close {
    position: absolute;
    top: 2rem;
    right: 2rem;
    background: rgba(255, 255, 255, 0.1);
    border: 2px solid rgba(255, 255, 255, 0.3);
    color: white;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    font-size: 1.5rem;
    transition: all 0.3s ease;
    z-index: 10000;
  }

  .modal-close:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: rotate(90deg);
  }

  .modal-content-img {
    max-width: 90vw;
    max-height: 90vh;
    border-radius: 12px;
    box-shadow: 0 30px 90px rgba(0, 0, 0, 0.8);
  }

  .section-dark {
    background: var(--bg-darker);
    padding: 6rem 2rem;
  }

  .section-light {
    background: var(--bg-main);
    padding: 6rem 2rem;
  }

  .container-narrow {
    max-width: 1000px;
    margin: 0 auto;
  }

  /* Creative Problem Cards Grid */
  .problems-creative-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    margin-top: 3rem;
    max-width: 1100px;
    margin-left: auto;
    margin-right: auto;
  }

  .problem-card-creative {
    position: relative;
    background: linear-gradient(135deg, rgba(17, 24, 39, 0.95) 0%, rgba(30, 41, 59, 0.9) 100%);
    border: 2px solid rgba(139, 92, 246, 0.25);
    border-radius: 20px;
    padding: 2.5rem;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .problem-card-creative::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, rgba(139, 92, 246, 0.8), rgba(168, 85, 247, 0.6));
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s ease;
  }

  .problem-card-creative:hover::before {
    transform: scaleX(1);
  }

  .problem-card-creative:hover {
    border-color: rgba(139, 92, 246, 0.5);
    transform: translateY(-8px);
    box-shadow: 0 20px 60px rgba(139, 92, 246, 0.3);
  }

  /* Accent cards for emphasis */
  .problem-primary {
    grid-column: span 1;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.15) 0%, rgba(124, 58, 237, 0.1) 100%);
    border-color: rgba(139, 92, 246, 0.4);
  }

  .problem-accent {
    grid-column: span 1;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.12) 0%, rgba(124, 58, 237, 0.08) 100%);
    border-color: rgba(139, 92, 246, 0.35);
  }

  .problem-number {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    font-size: 4rem;
    font-weight: 900;
    color: rgba(139, 92, 246, 0.12);
    line-height: 1;
    font-family: 'Inter', sans-serif;
  }

  .problem-icon-creative {
    width: 80px;
    height: 80px;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.2), rgba(124, 58, 237, 0.15));
    border: 3px solid rgba(139, 92, 246, 0.4);
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    transition: all 0.3s ease;
  }

  .problem-card-creative:hover .problem-icon-creative {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.3), rgba(124, 58, 237, 0.25));
    border-color: rgba(168, 85, 247, 0.6);
    transform: scale(1.05) rotate(-5deg);
  }

  .problem-icon-creative i {
    font-size: 2.5rem;
    color: #a855f7;
  }

  .problem-card-creative h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: var(--text-primary);
    font-weight: 800;
    position: relative;
    z-index: 2;
  }

  .problem-card-creative p {
    color: var(--text-secondary);
    line-height: 1.7;
    font-size: 1.05rem;
    margin: 0;
    position: relative;
    z-index: 2;
  }

  .problem-card-creative .highlight {
    color: #a855f7;
    font-weight: 700;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.2), transparent);
    padding: 0.15rem 0.4rem;
    border-radius: 4px;
  }

  .problem-card-creative .highlight-negative {
    color: #f87171;
    font-weight: 700;
    background: rgba(248, 113, 113, 0.15);
    padding: 0.15rem 0.4rem;
    border-radius: 4px;
  }

  .problem-impact {
    margin-top: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(139, 92, 246, 0.25);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .impact-label {
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-secondary);
    font-weight: 600;
  }

  .impact-value {
    font-size: 1.5rem;
    font-weight: 900;
    background: linear-gradient(135deg, #a855f7, #f87171);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Stats Section - Symmetrical 2x2 Grid */
  .stats-section {
    background: var(--bg-main);
    padding: 6rem 2rem;
  }

  .stats-container {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    max-width: 900px;
    margin: 3rem auto 0;
  }

  .stat-card {
    background: linear-gradient(135deg, rgba(6, 182, 212, 0.1) 0%, rgba(30, 64, 175, 0.1) 100%);
    border: 2px solid rgba(6, 182, 212, 0.3);
    border-radius: 20px;
    padding: 3rem 2rem;
    text-align: center;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
    overflow: hidden;
  }

  .stat-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--secondary-brand), var(--primary-brand));
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s ease;
  }

  .stat-card:hover::before {
    transform: scaleX(1);
  }

  .stat-card:hover {
    border-color: var(--secondary-brand);
    transform: translateY(-10px);
    box-shadow: 0 20px 60px rgba(6, 182, 212, 0.4);
    background: linear-gradient(135deg, rgba(6, 182, 212, 0.15) 0%, rgba(30, 64, 175, 0.15) 100%);
  }

  .stat-number {
    display: block;
    font-size: 4rem;
    font-weight: 900;
    background: var(--gradient-brand);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 1rem;
    line-height: 1;
  }

  .stat-label {
    display: block;
    font-size: 1.1rem;
    color: var(--text-secondary);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* Hexagon arrangement container */
  .factors-hexagon {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    margin-top: 3rem;
    max-width: 1000px;
    margin-left: auto;
    margin-right: auto;
    padding: 2rem 1rem;
  }

  /* Hexagon rows */
  .hex-row {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
  }

  /* Individual factor items - rectangles */
  .factor-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    background: linear-gradient(135deg, rgba(6, 182, 212, 0.1), rgba(30, 64, 175, 0.1));
    border: 2px solid rgba(6, 182, 212, 0.3);
    border-radius: 12px;
    padding: 1.25rem 1.75rem;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
    min-width: 200px;
  }

  /* Glow effect */
  .factor-item::before {
    content: '';
    position: absolute;
    inset: -2px;
    background: linear-gradient(135deg, #06B6D4, #1E40AF);
    border-radius: 12px;
    opacity: 0;
    z-index: -1;
    filter: blur(8px);
    transition: opacity 0.4s ease;
  }

  .factor-item:hover {
    border-color: var(--secondary-brand);
    transform: translateY(-8px);
    box-shadow: 0 15px 40px rgba(6, 182, 212, 0.5);
    background: linear-gradient(135deg, rgba(6, 182, 212, 0.2), rgba(30, 64, 175, 0.2));
  }

  .factor-item:hover::before {
    opacity: 0.6;
  }

  .factor-item i {
    font-size: 1.75rem;
    color: var(--secondary-brand);
    flex-shrink: 0;
    transition: transform 0.3s ease;
  }

  .factor-item:hover i {
    transform: scale(1.15) rotate(10deg);
  }

  .factor-item span {
    font-weight: 600;
    color: var(--text-primary);
    font-size: 0.95rem;
    line-height: 1.3;
  }

  .cta-section {
    background: linear-gradient(135deg, var(--secondary-brand) 0%, var(--primary-brand) 100%);
    padding: 6rem 2rem;
    text-align: center;
  }

  .cta-content {
    max-width: 800px;
    margin: 0 auto;
  }

  .cta-content h2 {
    font-size: 3rem;
    color: white;
    margin-bottom: 1rem;
  }

  .cta-content > p {
    font-size: 1.3rem;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 2.5rem;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .cta-note {
    margin-top: 2rem;
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.95rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  /* Tablet Optimizations */
  @media (max-width: 1024px) {
    .hero-container-pricing {
      flex-direction: column;
      padding: 4rem 2rem 3rem;
      gap: 3rem;
    }

    .hero-content-pricing {
      max-width: 100%;
      text-align: center;
    }

    .hero-visual-pricing {
      min-width: auto;
      width: 100%;
      max-width: 600px;
    }

    .screenshots-collage {
      height: 400px;
    }

    .screenshot-1 {
      width: 240px;
      height: 180px;
    }

    .screenshot-2 {
      width: 220px;
      height: 165px;
    }

    .screenshot-3 {
      width: 230px;
      height: 172px;
    }

    .screenshot-4 {
      width: 200px;
      height: 150px;
    }

    /* Creative problem cards on tablet */
    .problems-creative-grid {
      gap: 1.5rem;
    }

    .problem-card-creative {
      padding: 2rem;
    }

    .problem-icon-creative {
      width: 70px;
      height: 70px;
    }

    .problem-icon-creative i {
      font-size: 2rem;
    }

    .problem-number {
      font-size: 3.5rem;
    }
  }

  @media (max-width: 768px) {
    .hero-pricing::before {
      width: 600px;
      height: 600px;
      top: -30%;
      right: -40%;
    }

    .hero-container-pricing {
      padding: 6.5rem 1.5rem 3rem;
    }

    .hero-content-pricing {
      text-align: left;
    }

    .hero-badge {
      font-size: 0.8rem !important;
      padding: 0.4rem 0.75rem !important;
    }

    .hero-content-pricing h1 {
      font-size: 1.75rem !important;
      line-height: 1.2 !important;
      margin-top: 1rem !important;
      margin-bottom: 1rem !important;
    }

    .hero-description {
      font-size: 0.95rem !important;
      line-height: 1.6 !important;
      margin-bottom: 1.5rem !important;
    }

    /* Mobile hero stats */
    .hero-stats-mini {
      display: flex;
      gap: 0.75rem;
      margin: 1.5rem 0;
      flex-wrap: wrap;
    }

    .mini-stat {
      flex: 1;
      min-width: calc(33.333% - 0.5rem);
      text-align: center;
      padding: 0.75rem 0.5rem;
      background: rgba(6, 182, 212, 0.1);
      border: 1px solid rgba(6, 182, 212, 0.3);
      border-radius: 8px;
    }

    .mini-stat strong {
      display: block;
      font-size: 1.1rem;
      color: var(--secondary-brand);
      margin-bottom: 0.25rem;
      font-weight: 700;
    }

    .mini-stat span {
      display: block;
      font-size: 0.7rem;
      color: var(--text-secondary);
      line-height: 1.2;
    }

    /* Mobile CTAs */
    .hero-cta-group {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
      margin: 1.5rem 0;
    }

    .hero-cta-group .btn {
      width: 100%;
      justify-content: center;
      padding: 0.875rem 1.25rem !important;
      font-size: 0.95rem !important;
    }

    .hero-trust {
      font-size: 0.85rem !important;
      text-align: center;
      margin-top: 1rem !important;
      color: var(--text-secondary) !important;
    }

    /* Hide screenshots on mobile for performance */
    .hero-visual-pricing {
      display: none;
    }

    /* Mobile Platform Showcase */
    .platform-showcase-section {
      padding: 3rem 1.5rem;
    }

    .screenshot-grid {
      gap: 3rem;
      margin-top: 2rem;
    }

    .screenshot-item {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .screenshot-reverse {
      direction: ltr;
    }

    .screenshot-caption h4 {
      font-size: 1.2rem;
      line-height: 1.3;
      margin-bottom: 0.75rem;
    }

    .screenshot-caption p {
      font-size: 0.95rem;
      line-height: 1.6;
      margin-bottom: 1rem;
    }

    .feature-benefits-inline {
      gap: 0.5rem;
    }

    .feature-benefits-inline span {
      font-size: 0.8rem;
      padding: 0.35rem 0.7rem;
    }

    /* Section headers on mobile */
    .section-header h2 {
      font-size: 1.75rem !important;
      line-height: 1.2 !important;
    }

    .section-header p {
      font-size: 0.95rem !important;
      line-height: 1.5 !important;
    }

    .section-badge {
      font-size: 0.8rem !important;
      padding: 0.4rem 0.75rem !important;
    }

    /* Mobile problem section */
    .section-dark {
      padding: 3rem 1.5rem;
    }

    /* Mobile creative problem cards */
    .problems-creative-grid {
      grid-template-columns: 1fr;
      gap: 1.25rem;
      margin-top: 2rem;
    }

    .problem-card-creative {
      padding: 1.75rem 1.25rem;
    }

    .problem-number {
      font-size: 2.5rem;
      top: 0.75rem;
      right: 0.75rem;
    }

    .problem-icon-creative {
      width: 55px;
      height: 55px;
      margin-bottom: 1rem;
    }

    .problem-icon-creative i {
      font-size: 1.5rem;
    }

    .problem-card-creative h3 {
      font-size: 1.2rem;
      margin-bottom: 0.65rem;
      line-height: 1.3;
    }

    .problem-card-creative p {
      font-size: 0.95rem;
      line-height: 1.6;
    }

    .problem-impact {
      margin-top: 1.25rem;
      padding-top: 1.25rem;
    }

    .impact-label {
      font-size: 0.75rem;
    }

    .impact-value {
      font-size: 1.2rem;
    }

    /* Mobile stats section */
    .stats-section {
      padding: 3rem 1.5rem;
    }

    .stats-container {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
      margin-top: 2rem;
    }

    .stat-card {
      padding: 1.75rem 1.25rem;
    }

    .stat-number {
      font-size: 2.5rem;
      margin-bottom: 0.5rem;
    }

    .stat-label {
      font-size: 0.85rem;
    }

    /* Mobile section-light */
    .section-light {
      padding: 3rem 1.5rem;
    }

    /* Mobile hexagon - stack vertically */
    .factors-hexagon {
      padding: 0.5rem;
      gap: 1rem;
      margin-top: 2rem;
    }

    .hex-row {
      flex-direction: column;
      gap: 0.75rem;
      width: 100%;
    }

    .factor-item {
      width: 100%;
      min-width: auto;
      padding: 1rem 1.25rem;
    }

    .factor-item i {
      font-size: 1.5rem;
    }

    .factor-item span {
      font-size: 0.9rem;
    }

    /* Mobile CTA section */
    .cta-section {
      padding: 3rem 1.5rem;
    }

    .cta-content h2 {
      font-size: 1.75rem;
      line-height: 1.2;
      margin-bottom: 0.75rem;
    }

    .cta-content > p {
      font-size: 1rem;
      line-height: 1.5;
      margin-bottom: 2rem;
    }

    .cta-buttons {
      flex-direction: column;
      gap: 0.75rem;
    }

    .cta-buttons .btn {
      width: 100%;
      padding: 0.875rem 1.25rem !important;
      font-size: 0.95rem !important;
    }

    .cta-note {
      flex-direction: column;
      gap: 0.5rem;
      font-size: 0.85rem;
      margin-top: 1.5rem;
    }

    .modal-close {
      top: 1rem;
      right: 1rem;
      width: 40px;
      height: 40px;
      font-size: 1.2rem;
    }

    .modal-content-img {
      max-width: 95vw;
      max-height: 85vh;
    }
  }
</style>
