---
import BaseLayout from '../components/layout/BaseLayout.astro';
import SectionHeader from '../components/sections/SectionHeader.astro';
import CTASection from '../components/sections/CTASection.astro';
import { generateSEO } from '../lib/seo/metadata';
import { generateOrganizationSchema, generateBreadcrumbSchema, generateServiceSchema } from '../lib/seo/jsonld';

const seo = generateSEO({
  title: 'AI Automation Services for Tourism Businesses',
  description: 'AI chatbots, phone bots, custom software & API integrations for tourism. 24/7 guest support, intelligent email management, dynamic pricing & seamless PMS integration. Transform your operations.',
  canonical: `${Astro.site}services/`,
  ogType: 'website'
});

const services = [
  {
    icon: 'fa-robot',
    title: 'AI Chatbots & Phone Bots',
    description: 'Custom-built conversational AI agents that handle guest communications 24/7 across text and voice channels.',
    features: [
      'Natural language processing in 100+ languages',
      'Voice AI for phone calls with human-like conversations',
      'Website chat, WhatsApp, SMS integration',
      'Booking, inquiries, and support automation',
      'Seamless handoff to human agents when needed'
    ],
    stats: { value: '80%', label: 'Inquiries Automated' },
    color: 'cyan'
  },
  {
    icon: 'fa-code',
    title: 'Custom Software Development',
    description: 'Purpose-built applications designed specifically for your business workflows and operational needs.',
    features: [
      'Dynamic pricing engines with AI optimization',
      'Custom booking and reservation systems',
      'Inventory and resource management tools',
      'Analytics dashboards and reporting platforms',
      'Mobile apps for staff and guests'
    ],
    stats: { value: '100%', label: 'Satisfaction Guaranteed' },
    color: 'purple'
  },
  {
    icon: 'fa-network-wired',
    title: 'API Integrations & Automation',
    description: 'Connect your entire tech stack with intelligent workflows that eliminate manual data entry and errors.',
    features: [
      'PMS, CRM, and booking engine integrations',
      'Payment processor and accounting software sync',
      'Channel manager and OTA connections',
      'Email and communication platform automation',
      'Custom middleware for legacy systems'
    ],
    stats: { value: '99.9%', label: 'Uptime SLA' },
    color: 'pink'
  },
  {
    icon: 'fa-envelope',
    title: 'AI Email Management',
    description: 'Intelligent email sorting, categorization, and auto-response systems that handle guest communications at scale.',
    features: [
      'Smart inbox organization and prioritization',
      'AI-generated responses with brand voice',
      'Automatic categorization and routing',
      'Sentiment analysis for urgent issues',
      'Integration with ticketing systems'
    ],
    stats: { value: '90%', label: 'Faster Response' },
    color: 'orange'
  },
  {
    icon: 'fa-project-diagram',
    title: 'Process Mapping & Optimization',
    description: 'Clear visualization of your workflows to identify inefficiencies and automation opportunities.',
    features: [
      'Complete workflow documentation',
      'Bottleneck identification and analysis',
      'Automation opportunity assessment',
      'ROI calculation for improvements',
      'Implementation roadmap creation'
    ],
    stats: { value: '40hrs', label: 'Saved Weekly' },
    color: 'green'
  },
  {
    icon: 'fa-graduation-cap',
    title: 'Training & Ongoing Support',
    description: 'Comprehensive onboarding and continuous support to ensure your team masters every system we build.',
    features: [
      'Hands-on training for all staff levels',
      'Custom documentation and video tutorials',
      'Dedicated support team during business hours',
      'Regular system optimization reviews',
      'Feature updates and enhancements'
    ],
    stats: { value: '100%', label: 'Team Trained' },
    color: 'blue'
  }
];

const processSteps = [
  { number: '01', title: 'Discovery', description: 'We map your processes, understand your challenges, and identify automation opportunities', icon: 'fa-search' },
  { number: '02', title: 'Design', description: 'Custom solution design tailored to your specific workflows and business goals', icon: 'fa-pencil-ruler' },
  { number: '03', title: 'Development', description: 'Agile development with regular check-ins and iterative feedback loops', icon: 'fa-code' },
  { number: '04', title: 'Deploy & Support', description: 'Smooth rollout, team training, and ongoing optimization and support', icon: 'fa-rocket' }
];

// Generate service schema data from services array
const serviceSchemaData = services.map(s => ({ name: s.title, description: s.description }));

const jsonLd = [
  generateOrganizationSchema(),
  generateBreadcrumbSchema([
    { name: 'Home', url: Astro.site?.href || '' },
    { name: 'Services', url: `${Astro.site}services/` }
  ]),
  ...generateServiceSchema(serviceSchemaData)
];
---

<BaseLayout seo={seo} jsonLd={jsonLd}>
  <!-- Desktop Hero - Premium Animated -->
  <section class="services-hero desktop-only">
    <div class="hero-bg-effects">
      <div class="gradient-orb orb-1"></div>
      <div class="gradient-orb orb-2"></div>
      <div class="gradient-orb orb-3"></div>
      <div class="grid-overlay"></div>
    </div>

    <div class="hero-container">
      <div class="hero-content">
        <span class="hero-badge animate-fade-in">
          <i class="fas fa-wand-magic-sparkles"></i>
          Custom AI Solutions for Tourism
        </span>

        <h1 class="hero-title animate-fade-in-up">
          Transform Your Operations with
          <span class="gradient-text-animated">Intelligent Automation</span>
        </h1>

        <p class="hero-description animate-fade-in-up delay-1">
          From AI chatbots and phone bots to custom software and API integrations—we build intelligent solutions that transform how tourism businesses operate
        </p>

        <div class="hero-cta-group animate-fade-in-up delay-2">
          <button onclick="openBookingModal()" class="btn-primary-glow">
            <span>Get Free Consultation</span>
            <i class="fas fa-arrow-right"></i>
          </button>
          <a href="#calculator" class="btn-secondary-outline">
            <i class="fas fa-calculator"></i>
            <span>Calculate Your ROI</span>
          </a>
        </div>
      </div>

      <!-- Desktop Visual - Service Icons Grid -->
      <div class="hero-visual animate-fade-in-up delay-2">
        <div class="visual-grid">
          <div class="visual-card" style="--delay: 0s">
            <div class="visual-icon cyan"><i class="fas fa-robot"></i></div>
            <span>AI Chatbots</span>
          </div>
          <div class="visual-card" style="--delay: 0.1s">
            <div class="visual-icon purple"><i class="fas fa-phone-volume"></i></div>
            <span>Voice AI</span>
          </div>
          <div class="visual-card" style="--delay: 0.2s">
            <div class="visual-icon pink"><i class="fas fa-code"></i></div>
            <span>Custom Software</span>
          </div>
          <div class="visual-card" style="--delay: 0.3s">
            <div class="visual-icon orange"><i class="fas fa-network-wired"></i></div>
            <span>API Integration</span>
          </div>
          <div class="visual-card" style="--delay: 0.4s">
            <div class="visual-icon green"><i class="fas fa-envelope"></i></div>
            <span>Email AI</span>
          </div>
          <div class="visual-card" style="--delay: 0.5s">
            <div class="visual-icon blue"><i class="fas fa-chart-line"></i></div>
            <span>Analytics</span>
          </div>
        </div>

        <!-- Floating Stats -->
        <div class="floating-stats">
          <div class="floating-stat stat-1">
            <span class="stat-value">30%</span>
            <span class="stat-label">More Bookings</span>
          </div>
          <div class="floating-stat stat-2">
            <span class="stat-value">40+</span>
            <span class="stat-label">Hours Saved/Week</span>
          </div>
          <div class="floating-stat stat-3">
            <span class="stat-value">24/7</span>
            <span class="stat-label">Availability</span>
          </div>
        </div>
      </div>
    </div>

    <div class="scroll-indicator">
      <div class="scroll-line"></div>
      <span>Scroll to explore</span>
    </div>
  </section>

  <!-- Mobile Hero - Video Background (like homepage) -->
  <section class="services-mobile-hero mobile-only">
    <div class="services-video-bg">
      <video autoplay muted playsinline loop id="services-bg-video" preload="metadata" poster="/images/mobile-hero-poster.jpg">
        <source src="/videos/mobile-hero.mp4" type="video/mp4">
      </video>
      <script is:inline>
        const servicesBgVideo = document.getElementById('services-bg-video');
        if (servicesBgVideo) {
          servicesBgVideo.addEventListener('loadedmetadata', function() {
            servicesBgVideo.currentTime = 7;
          });
        }
      </script>

      <!-- Gradient overlay -->
      <div class="services-hero-gradient"></div>

      <!-- Content overlay -->
      <div class="services-hero-overlay">
        <h1 class="services-hero-headline">
          AI Solutions<br/>for Tourism
        </h1>
        <p class="services-hero-subhead">Chatbots, voice AI, custom software & integrations built for your business</p>
        <div class="services-hero-actions">
          <button onclick="openBookingModal()" class="services-hero-btn services-hero-btn-primary">
            Free Consultation
          </button>
          <a href="#calculator" class="services-hero-btn services-hero-btn-secondary">
            Calculate ROI
          </a>
        </div>
      </div>
    </div>

    <!-- Service Icons Grid - Below video on mobile -->
    <div class="services-icons-section">
      <div class="services-icons-grid">
        <div class="service-icon-card">
          <div class="service-icon-bubble cyan">
            <i class="fas fa-robot"></i>
          </div>
          <span>Chatbots</span>
        </div>
        <div class="service-icon-card">
          <div class="service-icon-bubble purple">
            <i class="fas fa-phone-volume"></i>
          </div>
          <span>Voice AI</span>
        </div>
        <div class="service-icon-card">
          <div class="service-icon-bubble pink">
            <i class="fas fa-code"></i>
          </div>
          <span>Software</span>
        </div>
        <div class="service-icon-card">
          <div class="service-icon-bubble orange">
            <i class="fas fa-network-wired"></i>
          </div>
          <span>APIs</span>
        </div>
        <div class="service-icon-card">
          <div class="service-icon-bubble green">
            <i class="fas fa-envelope"></i>
          </div>
          <span>Email AI</span>
        </div>
        <div class="service-icon-card">
          <div class="service-icon-bubble blue">
            <i class="fas fa-chart-line"></i>
          </div>
          <span>Analytics</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Premium Services Grid -->
  <section class="services-premium-section">
    <div class="section-container">
      <div class="section-header-premium">
        <span class="section-badge-glow">
          <i class="fas fa-cubes"></i>
          What We Build
        </span>
        <h2>Custom AI & Automation Solutions</h2>
        <p>Tailored technology that solves your specific business challenges</p>
      </div>

      <div class="services-premium-grid">
        {services.map((service, index) => (
          <div class="service-card-premium" data-color={service.color} style={`--delay: ${index * 0.1}s`}>
            <div class="card-glow"></div>
            <div class="card-content">
              <div class="service-header">
                <div class="service-icon-wrapper">
                  <div class="icon-glow"></div>
                  <i class={`fas ${service.icon}`}></i>
                </div>
                <div class="service-stat">
                  <span class="stat-value">{service.stats.value}</span>
                  <span class="stat-label">{service.stats.label}</span>
                </div>
              </div>

              <h3>{service.title}</h3>
              <p>{service.description}</p>

              <ul class="feature-list">
                {service.features.map((feature) => (
                  <li>
                    <i class="fas fa-check"></i>
                    <span>{feature}</span>
                  </li>
                ))}
              </ul>

              <button onclick="openBookingModal()" class="card-cta">
                <span>Learn More</span>
                <i class="fas fa-arrow-right"></i>
              </button>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Premium Process Section -->
  <section class="process-premium-section">
    <div class="section-container">
      <div class="section-header-premium centered">
        <span class="section-badge-glow">
          <i class="fas fa-route"></i>
          How We Work
        </span>
        <h2>Our Development Process</h2>
        <p>From discovery to deployment, we follow a proven process that delivers results</p>
      </div>

      <div class="process-timeline">
        {processSteps.map((step, index) => (
          <div class="process-step-premium" style={`--index: ${index}`}>
            <div class="step-connector">
              <div class="connector-line"></div>
              <div class="connector-dot"></div>
            </div>
            <div class="step-card">
              <div class="step-number">{step.number}</div>
              <div class="services-step-icon">
                <i class={`fas ${step.icon}`}></i>
              </div>
              <h4>{step.title}</h4>
              <p>{step.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Calculator Section - Embedded -->
  <section class="calculator-section" id="calculator">
    <div class="section-container">
      <div class="calculator-wrapper">
        <div class="calculator-header">
          <span class="section-badge-glow">
            <i class="fas fa-calculator"></i>
            Free ROI Analysis
          </span>
          <h2>Calculate Your <span class="gradient-text-animated">Automation Savings</span></h2>
          <p>See how much time and money your tourism business could save with AI agents and automation</p>
        </div>

        <div class="calculator-embed-wrapper">
          <iframe
            src="/mobile-calculator.html?embed=true"
            class="calculator-embed"
            title="ROI Calculator"
            loading="lazy"
          ></iframe>
        </div>

        <div class="calculator-cta">
          <a href="/calculator" class="calc-fullscreen-link">
            <i class="fas fa-expand"></i>
            Open Full Calculator
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Trust Indicators -->
  <section class="trust-section">
    <div class="section-container">
      <div class="trust-grid">
        <div class="trust-item">
          <div class="trust-icon">
            <i class="fas fa-shield-alt"></i>
          </div>
          <h4>GDPR Compliant</h4>
          <p>Full UK & EU data protection compliance</p>
        </div>
        <div class="trust-item">
          <div class="trust-icon">
            <i class="fas fa-lock"></i>
          </div>
          <h4>Bank-Level Security</h4>
          <p>AES-256 encryption for all data</p>
        </div>
        <div class="trust-item">
          <div class="trust-icon">
            <i class="fas fa-server"></i>
          </div>
          <h4>99.9% Uptime</h4>
          <p>Enterprise-grade reliability</p>
        </div>
        <div class="trust-item">
          <div class="trust-icon">
            <i class="fas fa-headset"></i>
          </div>
          <h4>Dedicated Support</h4>
          <p>Expert help when you need it</p>
        </div>
      </div>
    </div>
  </section>

  <CTASection
    title="Ready to Transform Your Operations?"
    description="Book a free consultation and discover how AI automation can revolutionize your tourism business"
  />
</BaseLayout>

<style>
  /* ==================== HERO SECTION - DESKTOP OPTIMIZED ==================== */
  .services-hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 10rem 4rem 6rem;
    overflow: hidden;
    background: linear-gradient(180deg, #000000 0%, #050810 50%, #0a0e1a 100%);
  }

  .hero-bg-effects {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.6;
    animation: orbFloat 20s ease-in-out infinite;
  }

  .orb-1 {
    width: 800px;
    height: 800px;
    background: radial-gradient(circle, rgba(6, 182, 212, 0.4) 0%, transparent 70%);
    top: -20%;
    right: -15%;
    animation-delay: 0s;
  }

  .orb-2 {
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(139, 92, 246, 0.35) 0%, transparent 70%);
    bottom: -30%;
    left: -15%;
    animation-delay: -7s;
  }

  .orb-3 {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(236, 72, 153, 0.25) 0%, transparent 70%);
    top: 40%;
    left: 30%;
    transform: translate(-50%, -50%);
    animation-delay: -14s;
  }

  @keyframes orbFloat {
    0%, 100% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(30px, -50px) scale(1.1); }
    50% { transform: translate(-20px, 30px) scale(0.9); }
    75% { transform: translate(-40px, -20px) scale(1.05); }
  }

  .grid-overlay {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(6, 182, 212, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(6, 182, 212, 0.03) 1px, transparent 1px);
    background-size: 80px 80px;
    mask-image: radial-gradient(ellipse at center, black 30%, transparent 80%);
  }

  .hero-container {
    position: relative;
    z-index: 1;
    max-width: 1400px;
    width: 100%;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .hero-content {
    text-align: left;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    background: rgba(6, 182, 212, 0.1);
    border: 1px solid rgba(6, 182, 212, 0.3);
    border-radius: 50px;
    font-size: 0.9rem;
    font-weight: 600;
    color: #06B6D4;
    margin-bottom: 2rem;
  }

  .hero-badge i {
    font-size: 0.85rem;
  }

  .hero-title {
    font-family: 'Sulphur Point', sans-serif;
    font-size: clamp(3rem, 5vw, 4.5rem);
    font-weight: 700;
    line-height: 1.05;
    color: #fff;
    margin-bottom: 1.5rem;
  }

  .gradient-text-animated {
    display: block;
    background: linear-gradient(135deg, #06B6D4, #8B5CF6, #EC4899, #06B6D4);
    background-size: 300% 300%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradientFlow 6s ease infinite;
  }

  @keyframes gradientFlow {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  .hero-description {
    font-size: 1.25rem;
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.7);
    max-width: 550px;
    margin-bottom: 2.5rem;
  }

  .hero-cta-group {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .btn-primary-glow {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1.1rem 2.25rem;
    background: linear-gradient(135deg, #06B6D4, #8B5CF6);
    border: none;
    border-radius: 12px;
    color: white;
    font-size: 1.05rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 20px rgba(6, 182, 212, 0.3);
  }

  .btn-primary-glow:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 40px rgba(6, 182, 212, 0.5);
  }

  .btn-secondary-outline {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1.1rem 2.25rem;
    background: transparent;
    border: 2px solid rgba(255, 255, 255, 0.2);
    border-radius: 12px;
    color: white;
    font-size: 1.05rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .btn-secondary-outline:hover {
    border-color: #06B6D4;
    background: rgba(6, 182, 212, 0.1);
    transform: translateY(-3px);
  }

  /* Hero Visual - Desktop Only */
  .hero-visual {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .visual-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
    position: relative;
    z-index: 1;
  }

  .visual-card {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 20px;
    padding: 1.75rem 1.5rem;
    text-align: center;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    animation: cardFloat 0.8s ease forwards;
    animation-delay: var(--delay);
    opacity: 0;
    transform: translateY(30px);
  }

  .visual-card:hover {
    transform: translateY(-8px) scale(1.02);
    border-color: rgba(255, 255, 255, 0.2);
    background: rgba(255, 255, 255, 0.06);
  }

  @keyframes cardFloat {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .visual-icon {
    width: 64px;
    height: 64px;
    margin: 0 auto 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 16px;
    position: relative;
  }

  .visual-icon::before {
    content: '';
    position: absolute;
    inset: -8px;
    border-radius: 24px;
    opacity: 0.3;
    filter: blur(12px);
    transition: opacity 0.3s ease;
  }

  .visual-card:hover .visual-icon::before {
    opacity: 0.5;
  }

  .visual-icon.cyan {
    background: rgba(6, 182, 212, 0.15);
  }
  .visual-icon.cyan::before { background: #06B6D4; }
  .visual-icon.cyan i { color: #06B6D4; }

  .visual-icon.purple {
    background: rgba(139, 92, 246, 0.15);
  }
  .visual-icon.purple::before { background: #8B5CF6; }
  .visual-icon.purple i { color: #8B5CF6; }

  .visual-icon.pink {
    background: rgba(236, 72, 153, 0.15);
  }
  .visual-icon.pink::before { background: #EC4899; }
  .visual-icon.pink i { color: #EC4899; }

  .visual-icon.orange {
    background: rgba(251, 146, 60, 0.15);
  }
  .visual-icon.orange::before { background: #FB923C; }
  .visual-icon.orange i { color: #FB923C; }

  .visual-icon.green {
    background: rgba(16, 185, 129, 0.15);
  }
  .visual-icon.green::before { background: #10B981; }
  .visual-icon.green i { color: #10B981; }

  .visual-icon.blue {
    background: rgba(59, 130, 246, 0.15);
  }
  .visual-icon.blue::before { background: #3B82F6; }
  .visual-icon.blue i { color: #3B82F6; }

  .visual-icon i {
    font-size: 1.5rem;
    position: relative;
    z-index: 1;
  }

  .visual-card span {
    font-size: 0.9rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.8);
  }

  /* Floating Stats */
  .floating-stats {
    position: absolute;
    inset: -40px;
    pointer-events: none;
  }

  .floating-stat {
    position: absolute;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 1rem 1.25rem;
    text-align: center;
    animation: floatBounce 4s ease-in-out infinite;
  }

  .floating-stat .stat-value {
    display: block;
    font-family: 'Sulphur Point', sans-serif;
    font-size: 1.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #06B6D4, #8B5CF6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .floating-stat .stat-label {
    font-size: 0.7rem;
    color: rgba(255, 255, 255, 0.6);
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .floating-stat.stat-1 {
    top: 0;
    left: -20px;
    animation-delay: 0s;
  }

  .floating-stat.stat-2 {
    bottom: 20%;
    right: -30px;
    animation-delay: -1.5s;
  }

  .floating-stat.stat-3 {
    bottom: -10px;
    left: 30%;
    animation-delay: -3s;
  }

  @keyframes floatBounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-12px); }
  }

  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    color: rgba(255, 255, 255, 0.5);
    font-size: 0.8rem;
  }

  .scroll-line {
    width: 1px;
    height: 40px;
    background: linear-gradient(180deg, rgba(6, 182, 212, 0.8), transparent);
    animation: scrollPulse 2s ease-in-out infinite;
  }

  @keyframes scrollPulse {
    0%, 100% { opacity: 0.5; transform: scaleY(1); }
    50% { opacity: 1; transform: scaleY(1.2); }
  }

  /* Animations */
  .animate-fade-in {
    animation: fadeIn 0.8s ease forwards;
  }

  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease forwards;
  }

  .delay-1 { animation-delay: 0.2s; opacity: 0; }
  .delay-2 { animation-delay: 0.4s; opacity: 0; }
  .delay-3 { animation-delay: 0.6s; opacity: 0; }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ==================== SERVICES SECTION ==================== */
  .services-premium-section {
    padding: 6rem 2rem;
    background: linear-gradient(180deg, #0a0e1a 0%, #000 100%);
  }

  .section-container {
    max-width: 1400px;
    margin: 0 auto;
  }

  .section-header-premium {
    margin-bottom: 4rem;
  }

  .section-header-premium.centered {
    text-align: center;
  }

  .section-badge-glow {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    background: rgba(6, 182, 212, 0.1);
    border: 1px solid rgba(6, 182, 212, 0.2);
    border-radius: 50px;
    font-size: 0.85rem;
    font-weight: 600;
    color: #06B6D4;
    margin-bottom: 1rem;
  }

  .section-header-premium h2 {
    font-family: 'Sulphur Point', sans-serif;
    font-size: clamp(2rem, 4vw, 2.75rem);
    color: #fff;
    margin-bottom: 1rem;
  }

  .section-header-premium p {
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.6);
    max-width: 600px;
  }

  .section-header-premium.centered p {
    margin: 0 auto;
  }

  .services-premium-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));
    gap: 2rem;
  }

  .service-card-premium {
    position: relative;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 24px;
    padding: 2rem;
    transition: all 0.4s ease;
    overflow: hidden;
    animation: fadeInUp 0.6s ease forwards;
    animation-delay: var(--delay);
    opacity: 0;
  }

  .service-card-premium .card-glow {
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle at center, var(--glow-color, rgba(6, 182, 212, 0.1)) 0%, transparent 50%);
    opacity: 0;
    transition: opacity 0.4s ease;
    pointer-events: none;
  }

  .service-card-premium[data-color="cyan"] { --glow-color: rgba(6, 182, 212, 0.15); --accent: #06B6D4; }
  .service-card-premium[data-color="purple"] { --glow-color: rgba(139, 92, 246, 0.15); --accent: #8B5CF6; }
  .service-card-premium[data-color="pink"] { --glow-color: rgba(236, 72, 153, 0.15); --accent: #EC4899; }
  .service-card-premium[data-color="orange"] { --glow-color: rgba(251, 146, 60, 0.15); --accent: #FB923C; }
  .service-card-premium[data-color="green"] { --glow-color: rgba(16, 185, 129, 0.15); --accent: #10B981; }
  .service-card-premium[data-color="blue"] { --glow-color: rgba(59, 130, 246, 0.15); --accent: #3B82F6; }

  .service-card-premium:hover {
    transform: translateY(-8px);
    border-color: var(--accent, rgba(6, 182, 212, 0.3));
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
  }

  .service-card-premium:hover .card-glow {
    opacity: 1;
  }

  .card-content {
    position: relative;
    z-index: 1;
  }

  .service-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 1.5rem;
  }

  .service-icon-wrapper {
    position: relative;
    width: 60px;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 16px;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .service-icon-wrapper .icon-glow {
    position: absolute;
    inset: -10px;
    background: radial-gradient(circle, var(--accent, #06B6D4) 0%, transparent 70%);
    opacity: 0.2;
    filter: blur(10px);
    transition: opacity 0.3s ease;
  }

  .service-card-premium:hover .icon-glow {
    opacity: 0.4;
  }

  .service-icon-wrapper i {
    font-size: 1.5rem;
    color: var(--accent, #06B6D4);
  }

  .service-stat {
    text-align: right;
    padding: 0.5rem 1rem;
    background: rgba(255, 255, 255, 0.03);
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.05);
  }

  .service-stat .stat-value {
    display: block;
    font-family: 'Sulphur Point', sans-serif;
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--accent, #06B6D4);
  }

  .service-stat .stat-label {
    font-size: 0.7rem;
    color: rgba(255, 255, 255, 0.5);
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .service-card-premium h3 {
    font-family: 'Sulphur Point', sans-serif;
    font-size: 1.35rem;
    color: #fff;
    margin-bottom: 0.75rem;
  }

  .service-card-premium > .card-content > p {
    font-size: 0.95rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 1.5rem;
  }

  .feature-list {
    list-style: none;
    padding: 0;
    margin: 0 0 1.5rem;
  }

  .feature-list li {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    padding: 0.5rem 0;
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.7);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .feature-list li:last-child {
    border-bottom: none;
  }

  .feature-list li i {
    color: var(--accent, #06B6D4);
    font-size: 0.75rem;
    margin-top: 0.25rem;
  }

  .card-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.25rem;
    background: transparent;
    border: 1px solid var(--accent, rgba(6, 182, 212, 0.3));
    border-radius: 8px;
    color: var(--accent, #06B6D4);
    font-size: 0.9rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .card-cta:hover {
    background: var(--accent, #06B6D4);
    color: #000;
    transform: translateX(4px);
  }

  /* ==================== PROCESS SECTION ==================== */
  .process-premium-section {
    padding: 6rem 2rem;
    background: #000;
    position: relative;
  }

  .process-timeline {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    margin-top: 3rem;
    position: relative;
  }

  .process-timeline::before {
    content: '';
    position: absolute;
    top: 80px;
    left: 12.5%;
    right: 12.5%;
    height: 2px;
    background: linear-gradient(90deg, rgba(6, 182, 212, 0.3), rgba(139, 92, 246, 0.3), rgba(236, 72, 153, 0.3), rgba(6, 182, 212, 0.3));
  }

  .process-step-premium {
    position: relative;
    text-align: center;
    opacity: 0;
    animation: fadeInUp 0.6s ease forwards;
    animation-delay: calc(var(--index) * 0.15s);
  }

  .step-connector {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-bottom: 1.5rem;
  }

  .connector-dot {
    width: 20px;
    height: 20px;
    background: linear-gradient(135deg, #06B6D4, #8B5CF6);
    border-radius: 50%;
    position: relative;
    z-index: 1;
  }

  .connector-dot::before {
    content: '';
    position: absolute;
    inset: -6px;
    border-radius: 50%;
    background: rgba(6, 182, 212, 0.2);
    animation: dotPulse 2s ease-in-out infinite;
  }

  @keyframes dotPulse {
    0%, 100% { transform: scale(1); opacity: 0.5; }
    50% { transform: scale(1.3); opacity: 0.2; }
  }

  .step-card {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 20px;
    padding: 2rem 1.5rem;
    transition: all 0.3s ease;
  }

  .step-card:hover {
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(6, 182, 212, 0.3);
    transform: translateY(-4px);
  }

  .step-number {
    font-family: 'Sulphur Point', sans-serif;
    font-size: 2.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #06B6D4, #8B5CF6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 1rem;
  }

  .services-step-icon {
    width: 50px;
    height: 50px;
    margin: 0 auto 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(6, 182, 212, 0.1);
    border-radius: 12px;
  }

  .services-step-icon i {
    font-size: 1.25rem;
    color: #06B6D4;
  }

  .step-card h4 {
    font-family: 'Sulphur Point', sans-serif;
    font-size: 1.25rem;
    color: #fff;
    margin-bottom: 0.75rem;
  }

  .step-card p {
    font-size: 0.9rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.6);
  }

  /* ==================== CALCULATOR SECTION ==================== */
  .calculator-section {
    padding: 6rem 2rem;
    background: linear-gradient(180deg, #000 0%, #050810 50%, #0a0e1a 100%);
    position: relative;
  }

  .calculator-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(6, 182, 212, 0.15) 0%, transparent 70%);
    filter: blur(80px);
    pointer-events: none;
  }

  .calculator-wrapper {
    position: relative;
    z-index: 1;
    max-width: 1100px;
    margin: 0 auto;
  }

  .calculator-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .calculator-header h2 {
    font-family: 'Sulphur Point', sans-serif;
    font-size: clamp(2rem, 4vw, 2.5rem);
    color: #fff;
    margin-bottom: 1rem;
  }

  .calculator-header p {
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.6);
  }

  .calculator-embed-wrapper {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 24px;
    overflow: hidden;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
  }

  .calculator-embed {
    width: 100%;
    min-height: 1200px;
    border: none;
    display: block;
  }

  .calculator-cta {
    display: flex;
    justify-content: center;
    margin-top: 1.5rem;
  }

  .calc-fullscreen-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.95rem;
    text-decoration: none;
    padding: 0.75rem 1.5rem;
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 8px;
    transition: all 0.3s ease;
  }

  .calc-fullscreen-link:hover {
    color: #06B6D4;
    border-color: rgba(6, 182, 212, 0.3);
    background: rgba(6, 182, 212, 0.1);
  }

  /* ==================== TRUST SECTION ==================== */
  .trust-section {
    padding: 4rem 2rem;
    background: #0a0e1a;
  }

  .trust-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
  }

  .trust-item {
    text-align: center;
    padding: 1.5rem;
  }

  .trust-icon {
    width: 60px;
    height: 60px;
    margin: 0 auto 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(6, 182, 212, 0.1);
    border-radius: 16px;
    border: 1px solid rgba(6, 182, 212, 0.2);
  }

  .trust-icon i {
    font-size: 1.5rem;
    color: #06B6D4;
  }

  .trust-item h4 {
    font-size: 1rem;
    color: #fff;
    margin-bottom: 0.5rem;
  }

  .trust-item p {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.5);
  }

  /* ==================== RESPONSIVE ==================== */
  @media (max-width: 1024px) {
    .services-premium-grid {
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    }

    .process-timeline {
      grid-template-columns: repeat(2, 1fr);
    }

    .process-timeline::before {
      display: none;
    }

    .calc-results {
      grid-template-columns: 1fr;
    }

    .result-card.highlight {
      grid-column: span 1;
    }

    .trust-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 1024px) {
    .hero-container {
      grid-template-columns: 1fr;
      gap: 3rem;
      text-align: center;
    }

    .hero-content {
      text-align: center;
    }

    .hero-description {
      margin: 0 auto 2.5rem;
    }

    .hero-cta-group {
      justify-content: center;
    }

    .hero-visual {
      max-width: 500px;
      margin: 0 auto;
    }

    .visual-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
    }

    .floating-stats {
      display: none;
    }
  }

  /* ==================== DESKTOP/MOBILE VISIBILITY UTILITIES ==================== */
  .desktop-only {
    display: block;
  }

  .mobile-only {
    display: none !important;
  }

  @media (max-width: 768px) {
    .desktop-only {
      display: none !important;
    }

    .mobile-only {
      display: block !important;
    }
  }

  /* ==================== MOBILE VIDEO HERO (Services Page) ==================== */
  .services-mobile-hero {
    display: none;
  }

  @media (max-width: 768px) {
    .services-mobile-hero {
      display: block !important;
      position: relative;
      width: 100%;
      min-height: 100vh;
      background: #000;
      /* Remove fixed height - icons section handles scroll */
    }

    /* Video Background Container - Fixed full viewport like homepage */
    .services-video-bg {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      overflow: hidden;
      z-index: -1;
      margin: 0;
      padding: 0;
      border: none;
    }

    .services-video-bg video {
      width: 100vw;
      height: 100vh;
      object-fit: cover;
      margin: 0;
      padding: 0;
      border: none;
    }

    /* Gradient Overlay - Same as homepage */
    .services-hero-gradient {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 60%;
      background: linear-gradient(
        to bottom,
        transparent 0%,
        rgba(0, 0, 0, 0.35) 15%,
        rgba(0, 0, 0, 0.7) 40%,
        rgba(0, 0, 0, 0.88) 70%,
        rgba(0, 0, 0, 0.97) 100%
      );
      z-index: 2;
    }

    /* Content Overlay - Bottom left like homepage */
    .services-hero-overlay {
      position: absolute;
      bottom: 8rem;
      left: 0;
      right: 0;
      padding: 0 1.5rem;
      padding-bottom: max(1rem, env(safe-area-inset-bottom, 1rem));
      z-index: 3;
      text-align: left;
      animation: fadeInUp 1s cubic-bezier(0.16, 1, 0.3, 1) 0.3s backwards;
    }

    .services-hero-headline {
      font-size: 2.75rem;
      line-height: 1.1;
      font-weight: 800;
      color: white;
      margin: 0 0 1rem 0;
      padding: 0;
      letter-spacing: -0.02em;
      text-align: left;
    }

    .services-hero-subhead {
      font-size: 1rem;
      line-height: 1.5;
      font-weight: 400;
      color: rgba(255, 255, 255, 0.85);
      margin-bottom: 1.5rem;
      text-align: left;
    }

    /* CTA Buttons - Left aligned like homepage */
    .services-hero-actions {
      display: flex;
      gap: 0.75rem;
      align-items: center;
      justify-content: flex-start;
      flex-wrap: nowrap;
    }

    .services-hero-btn {
      padding: 0.75rem 1.25rem;
      border-radius: 8px;
      font-size: 0.85rem;
      font-weight: 600;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      white-space: nowrap;
      flex: 1;
      text-align: center;
      border: none;
    }

    .services-hero-btn-primary {
      background: linear-gradient(135deg, #6366f1 0%, #818cf8 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(99, 102, 241, 0.4);
      border: none;
    }

    .services-hero-btn-primary:active {
      transform: scale(0.98);
    }

    .services-hero-btn-secondary {
      background: transparent;
      color: white;
      border: 1.5px solid rgba(255, 255, 255, 0.4);
    }

    .services-hero-btn-secondary:active {
      background: rgba(255, 255, 255, 0.1);
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Service Icons Section - Positioned below 100vh video */
    .services-icons-section {
      position: relative;
      margin-top: 100vh;
      background: linear-gradient(180deg, #000 0%, #0a0e1a 100%);
      padding: 2rem 1rem 1.5rem;
      z-index: 1;
    }

    .services-icons-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      max-width: 360px;
      margin: 0 auto;
    }

    .service-icon-card {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.5rem;
      text-align: center;
    }

    .service-icon-bubble {
      width: 52px;
      height: 52px;
      border-radius: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: transform 0.2s ease;
    }

    .service-icon-bubble i {
      font-size: 1.25rem;
    }

    .service-icon-card span {
      font-size: 0.7rem;
      font-weight: 600;
      color: rgba(255, 255, 255, 0.8);
      letter-spacing: 0.02em;
    }

    /* Icon Colors */
    .service-icon-bubble.cyan {
      background: rgba(6, 182, 212, 0.15);
      border: 1px solid rgba(6, 182, 212, 0.3);
    }
    .service-icon-bubble.cyan i { color: #06B6D4; }

    .service-icon-bubble.purple {
      background: rgba(139, 92, 246, 0.15);
      border: 1px solid rgba(139, 92, 246, 0.3);
    }
    .service-icon-bubble.purple i { color: #8B5CF6; }

    .service-icon-bubble.pink {
      background: rgba(236, 72, 153, 0.15);
      border: 1px solid rgba(236, 72, 153, 0.3);
    }
    .service-icon-bubble.pink i { color: #EC4899; }

    .service-icon-bubble.orange {
      background: rgba(251, 146, 60, 0.15);
      border: 1px solid rgba(251, 146, 60, 0.3);
    }
    .service-icon-bubble.orange i { color: #FB923C; }

    .service-icon-bubble.green {
      background: rgba(16, 185, 129, 0.15);
      border: 1px solid rgba(16, 185, 129, 0.3);
    }
    .service-icon-bubble.green i { color: #10B981; }

    .service-icon-bubble.blue {
      background: rgba(59, 130, 246, 0.15);
      border: 1px solid rgba(59, 130, 246, 0.3);
    }
    .service-icon-bubble.blue i { color: #3B82F6; }
  }

  /* Small mobile adjustments */
  @media (max-width: 375px) {
    .services-video-bg {
      height: 80vh;
      min-height: 450px;
    }

    .services-hero-headline {
      font-size: 2.25rem;
    }

    .services-hero-subhead {
      font-size: 0.9rem;
    }

    .services-hero-actions {
      flex-direction: column;
      gap: 0.625rem;
    }

    .services-hero-btn {
      width: 100%;
      max-width: 260px;
    }

    .services-icons-grid {
      gap: 0.75rem;
    }

    .service-icon-bubble {
      width: 46px;
      height: 46px;
    }

    .service-icon-bubble i {
      font-size: 1.1rem;
    }

    .service-icon-card span {
      font-size: 0.65rem;
    }
  }

  /* ==========================================================================
     MOBILE STYLES - CRO OPTIMIZED (max-width: 768px)

     CRO Best Practices Applied:
     - Clear visual hierarchy with proper spacing rhythm (8px base)
     - Thumb-friendly tap targets (min 48px)
     - F-pattern reading optimization
     - High contrast CTAs
     - Reduced cognitive load
     - Trust signals prominently placed
     ========================================================================== */
  @media (max-width: 768px) {

    /* ========== SERVICES SECTION - Below Video Hero ========== */
    .services-premium-section {
      padding: 2rem 1rem;
    }

    .process-premium-section {
      padding: 2rem 1rem 1.5rem;
    }

    .calculator-section {
      padding: 2rem 1rem;
    }

    /* Section Headers - Consistent rhythm */
    .section-header-premium {
      margin-bottom: 1.5rem;
      text-align: center;
    }

    .section-badge-glow {
      font-size: 0.65rem;
      padding: 0.35rem 0.7rem;
      margin-bottom: 0.625rem;
      letter-spacing: 0.03em;
    }

    .section-badge-glow i {
      font-size: 0.6rem;
    }

    .section-header-premium h2 {
      font-size: 1.375rem;
      line-height: 1.25;
      margin-bottom: 0.5rem;
      letter-spacing: -0.01em;
    }

    .section-header-premium p {
      font-size: 0.85rem;
      line-height: 1.5;
      color: rgba(255, 255, 255, 0.65);
      max-width: 300px;
      margin: 0 auto;
    }

    /* Service Cards - Scannable format */
    .services-premium-grid {
      grid-template-columns: 1fr !important;
      gap: 1rem;
    }

    .service-card-premium {
      padding: 1.25rem;
      border-radius: 14px;
      animation: none !important;
      opacity: 1 !important;
      border: 1px solid rgba(255, 255, 255, 0.08);
    }

    .service-card-premium:hover {
      transform: none;
    }

    .service-card-premium .card-glow {
      display: none; /* Remove for performance */
    }

    /* Card Header - Icon + Stats inline */
    .service-header {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
      gap: 0.75rem;
      margin-bottom: 0.875rem;
    }

    .service-icon-wrapper {
      width: 44px;
      height: 44px;
      border-radius: 10px;
      flex-shrink: 0;
    }

    .service-icon-wrapper .icon-glow {
      display: none;
    }

    .service-icon-wrapper i {
      font-size: 1.1rem;
    }

    .service-stat {
      text-align: right;
      padding: 0.4rem 0.6rem;
      border-radius: 8px;
      background: rgba(var(--accent-rgb, 6, 182, 212), 0.08);
    }

    .service-stat .stat-value {
      font-size: 1rem;
      display: block;
      line-height: 1.1;
    }

    .service-stat .stat-label {
      font-size: 0.55rem;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      opacity: 0.7;
    }

    /* Card Content */
    .service-card-premium h3 {
      font-size: 1.05rem;
      margin-bottom: 0.5rem;
      line-height: 1.25;
    }

    .service-card-premium > .card-content > p {
      font-size: 0.8rem;
      line-height: 1.5;
      margin-bottom: 0.875rem;
      color: rgba(255, 255, 255, 0.6);
    }

    /* Feature List - Compact */
    .feature-list {
      margin-bottom: 1rem;
    }

    .feature-list li {
      font-size: 0.75rem;
      padding: 0.3rem 0;
      border-bottom: 1px solid rgba(255, 255, 255, 0.04);
    }

    .feature-list li:last-child {
      border-bottom: none;
    }

    .feature-list li i {
      font-size: 0.55rem;
      width: 14px;
    }

    /* Card CTA - Full width, prominent */
    .card-cta {
      width: 100%;
      justify-content: center;
      padding: 0.75rem 1rem;
      font-size: 0.8rem;
      min-height: 44px;
      border-radius: 8px;
      font-weight: 600;
    }

    /* ========== PROCESS TIMELINE ========== */
    .process-timeline {
      grid-template-columns: 1fr !important;
      gap: 0.875rem;
      margin-top: 1.5rem;
    }

    .process-timeline::before {
      display: none;
    }

    .process-step-premium {
      animation: none !important;
      opacity: 1 !important;
    }

    .step-connector {
      display: none;
    }

    /* Step Cards - Horizontal layout for mobile */
    .step-card {
      padding: 1rem 1.125rem;
      text-align: left;
      border-radius: 12px;
      display: flex;
      align-items: flex-start;
      gap: 0.875rem;
    }

    .step-number {
      font-size: 1.5rem;
      margin-bottom: 0;
      line-height: 1;
      flex-shrink: 0;
      width: 36px;
    }

    .services-step-icon {
      display: none; /* Hide on mobile for cleaner look */
    }

    .step-card h4 {
      font-size: 0.95rem;
      margin-bottom: 0.25rem;
      line-height: 1.2;
    }

    .step-card p {
      font-size: 0.75rem;
      line-height: 1.45;
      color: rgba(255, 255, 255, 0.6);
    }

    /* ========== CALCULATOR SECTION ========== */
    .calculator-header {
      margin-bottom: 1.5rem;
    }

    .calculator-header h2 {
      font-size: 1.375rem;
      margin-bottom: 0.5rem;
    }

    .calculator-header p {
      font-size: 0.85rem;
      max-width: 300px;
      margin: 0 auto;
    }

    .calculator-embed-wrapper {
      border-radius: 12px;
      border: 1px solid rgba(255, 255, 255, 0.08);
    }

    .calculator-embed {
      min-height: 650px;
    }

    .calculator-cta {
      margin-top: 1rem;
    }

    .calc-fullscreen-link {
      font-size: 0.75rem;
      padding: 0.5rem 0.875rem;
      border-radius: 6px;
    }

    /* ========== TRUST SECTION - Social Proof ========== */
    .trust-section {
      padding: 2rem 1rem;
      background: linear-gradient(180deg, #0a0e1a 0%, #000 100%);
    }

    .trust-grid {
      grid-template-columns: repeat(2, 1fr) !important; /* 2x2 grid for trust */
      gap: 0.625rem;
    }

    .trust-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 0.875rem 0.5rem;
      background: rgba(255, 255, 255, 0.02);
      border-radius: 10px;
      border: 1px solid rgba(255, 255, 255, 0.05);
    }

    .trust-icon {
      width: 40px;
      height: 40px;
      margin: 0 0 0.5rem 0;
      border-radius: 10px;
    }

    .trust-icon i {
      font-size: 1rem;
    }

    .trust-item h4 {
      font-size: 0.75rem;
      margin-bottom: 0.125rem;
      line-height: 1.2;
    }

    .trust-item p {
      font-size: 0.6rem;
      line-height: 1.3;
      color: rgba(255, 255, 255, 0.5);
    }
  }

  /* ========== SMALL MOBILE (max-width: 375px) ========== */
  @media (max-width: 375px) {
    .services-hero {
      padding: 5rem 1rem 2rem;
    }

    .hero-title {
      font-size: 1.5rem;
    }

    .hero-description {
      font-size: 0.85rem;
      max-width: 280px;
    }

    .hero-cta-group {
      max-width: 260px;
    }

    .section-header-premium h2 {
      font-size: 1.25rem;
    }

    .service-card-premium {
      padding: 1rem;
    }

    .trust-grid {
      gap: 0.5rem;
    }

    .trust-item {
      padding: 0.75rem 0.375rem;
    }
  }

  /* Original trust-item flex for reference - kept for larger mobile */
  @media (max-width: 768px) and (min-width: 480px) {
    .trust-item {
      flex-direction: row;
      text-align: left;
      gap: 0.75rem;
      padding: 0.75rem 1rem;
    }

    .trust-icon {
      margin: 0;
      flex-shrink: 0;
    }

    .trust-grid {
      grid-template-columns: 1fr 1fr !important;
    }
  }

  /* Keep the original trust-item that was here */
  @media (max-width: 479px) {
    .trust-item {
      display: flex;
      align-items: center;
      gap: 1rem;
      text-align: left;
      padding: 1rem;
      background: rgba(255, 255, 255, 0.02);
      border-radius: 12px;
      border: 1px solid rgba(255, 255, 255, 0.05);
    }

    .trust-icon {
      width: 50px;
      height: 50px;
      margin: 0;
      flex-shrink: 0;
    }

    .trust-icon i {
      font-size: 1.25rem;
    }

    .trust-item h4 {
      font-size: 0.95rem;
      margin-bottom: 0.25rem;
    }

    .trust-item p {
      font-size: 0.8rem;
    }
  }
</style>

